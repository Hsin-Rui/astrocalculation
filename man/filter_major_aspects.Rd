% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/astro_calculate_aspect.R
\name{filter_major_aspects}
\alias{filter_major_aspects}
\title{Filter aspect results to the four major aspects only}
\usage{
filter_major_aspects(aspect_result)
}
\arguments{
\item{aspect_result}{A data frame produced by \code{\link[=calculate_aspect]{calculate_aspect()}}.  Must
contain at minimum an \code{aspect} column of character strings.}
}
\value{
A data frame with only rows where \code{aspect} is one of
\code{"conjunction"}, \code{"square"}, \code{"trine"}, or \code{"opposition"}.  Column
structure is identical to the input.
}
\description{
Reduces the data frame returned by \code{\link[=calculate_aspect]{calculate_aspect()}} to rows whose
\code{aspect} column matches one of the four major aspects: conjunction, square,
trine, and opposition.  Sextile and any other non-major aspects are excluded.
}
\details{
This is the recommended entry point when semantic summaries are required for
the major-4 subset, as it pairs with \link{DescriptionEngine}\verb{$get_aspect_summary()}
without needing to filter again at the consumer.
}
