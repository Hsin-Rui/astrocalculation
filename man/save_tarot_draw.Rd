% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_user_data.R
\name{save_tarot_draw}
\alias{save_tarot_draw}
\title{Save a Tarot Draw to the Journal}
\usage{
save_tarot_draw(
  pool,
  user_id,
  card_id,
  interpretation_text = NULL,
  is_free_tier = TRUE
)
}
\arguments{
\item{pool}{A DBI/pool database connection.}

\item{user_id}{The \code{user_entity_id} of the authenticated user.}

\item{card_id}{Character identifier of the drawn card (e.g. card name).}

\item{interpretation_text}{Full interpretation text to persist (may be NULL).}

\item{is_free_tier}{Logical; TRUE = LLM credit consumed from the free 1/day quota.}
}
\value{
Invisibly, the number of rows inserted (always 1 on success).
}
\description{
Inserts a new row into \code{tarot_draws}, linking the draw to the authenticated
user and recording the LLM (or static) interpretation.  Uses a parameterized
query to prevent SQL injection (project standard).
}
