% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/astro_visualize_chart.R
\name{calculate_and_render_natal_chart}
\alias{calculate_and_render_natal_chart}
\title{Calculate planetary positions and render a natal chart to a temporary JPEG file}
\usage{
calculate_and_render_natal_chart(
  country,
  city,
  datetime,
  chart_name,
  selected_planets,
  width = 600,
  height = 600,
  pointsize = 24,
  res = 96
)
}
\arguments{
\item{country}{Character. Full country name (e.g., \code{"Taiwan"}).}

\item{city}{Character. City name (e.g., \code{"Taipei"}).}

\item{datetime}{POSIXct. Target datetime for the chart.}

\item{chart_name}{Character. Display name rendered on the chart.}

\item{selected_planets}{Character vector. Body names to include (e.g.,
\code{c("sun", "moon", "asc")}).}

\item{width}{Integer. JPEG width in pixels. Default 600.}

\item{height}{Integer. JPEG height in pixels. Default 600.}

\item{pointsize}{Integer. Base font size. Default 24.}

\item{res}{Integer. Resolution in ppi. Default 96.}
}
\value{
Absolute path (character) to the generated JPEG tempfile.
}
\description{
Full async-safe pipeline: city/timezone lookup, planet position calculation,
aspect computation, and JPEG rendering — all in one call. Designed to be
invoked inside a \code{future_promise()} worker so the main Shiny thread
never blocks. Only plain R objects are returned; no ggplot or R6 objects
cross the \code{future} process boundary.
}
